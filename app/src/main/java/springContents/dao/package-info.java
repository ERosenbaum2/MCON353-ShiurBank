/**
 * Provides Data Access Objects (DAOs) for database interaction.
 * <p>
 * This package contains classes that encapsulate database access logic,
 * including user management, series and recording data, participant approvals, and other
 * business operations. Each DAO supports typical CRUD and query operations for its
 * associated entity or feature.
 */
package springContents.dao;